import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Teste básico', (WidgetTester tester) async {
    // Teste vazio por enquanto - vamos focar no app primeiro
    expect(1 + 1, 2);
  });
}
